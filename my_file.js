//setting the integer to be the first
var finalSum = 0;

// for loop for the main and nested index.
for (var main_index = 0; main_index < finances.length; main_index++ ) {
    for(var nested_index = 0; nested_index < finances[main_index].length; nested_index++) {
    }
    // total variable for the sum of all the profit.
    var total = finalSum += (finances[main_index][1])
    var average = total / finances.length - 1
  

    
}
console.log('Total months: ' + main_index)
console.log('Total: ' + '$' + (total - 1))
console.log('Average:' + 'S' + Math.round(average))
